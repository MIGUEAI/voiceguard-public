name: Documentação Automática VoiceGuard

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  gerar_documentacao:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Configurar Python 3.13
        uses: actions/setup-python@v4
        with:
          python-version: 3.13

      - name: Instalar dependências
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install -r requirements.txt

      - name: Gerar relatório técnico atualizado
        run: |
          source venv/bin/activate
          python3 scripts/gerar_relatorio_tecnico.py

      - name: Atualizar índice da documentação
        run: |
          source venv/bin/activate
          python3 scripts/atualizar_indice_documentacao.py

      - name: Commit e push das alterações
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/Documentacao_Centralizada.md docs/Relatorio_Tecnico_Atualizado_*.md
          git commit -m "📚 Atualização automática da documentação técnica [ci skip]" || echo "Nada para commitar"
          git push origin main
